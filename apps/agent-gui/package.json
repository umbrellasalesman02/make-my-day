{
  "name": "agent-gui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "tsc -p tsconfig.json && node dist/start.js",
    "start:remote": "tsc -p tsconfig.json && node dist/quickstart.js",
    "preflight": "tsc -p tsconfig.json && node dist/start.js --preflight",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "bun": "catalog:",
    "typescript": "catalog:"
  }
}
